{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter server build && pnpm --filter web build",
  "outputDirectory": "apps/web/dist",
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x"
    }
  }
}

{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm: debug (backend)",
            "type": "shell",
            "command": "npm run debug",
            "options": {
                "cwd": "${workspaceFolder}/backend"
            },
            "problemMatcher": []
        }
    ]
}